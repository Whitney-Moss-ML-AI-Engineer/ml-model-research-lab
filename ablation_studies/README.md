# Ablation Studies

Remove or change one component at a time to determine whether it contributes meaningful value.

Examples:
- Remove attention
- Remove residual connections
- Remove data augmentation
- Remove selected features
- Compare optimizers
- Compare regularization

Report the baseline and controlled change together.
